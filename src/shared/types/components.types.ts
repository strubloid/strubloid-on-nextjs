/**
 * Component Props Types
 * Types for component props and component-specific interfaces
 */

/** Props for components that receive a Google Maps API key */
export interface IGoogleKeyProps {
    googleKey?: string;
}
