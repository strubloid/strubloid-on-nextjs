/**
 * Scrapbook Feature Components
 * Exports all scrapbook page components
 */

// These components will be migrated from components/scrapbook/ in subsequent updates
// For now, they remain accessible via the old path due to @components/* alias
// Future: Move actual files here and export them

// export { default as Header } from './Header';
// export { default as Body } from './Body';
// export { default as NewNote } from './NewNote';
